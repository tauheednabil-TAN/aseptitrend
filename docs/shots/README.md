# Screenshots for the overview page

Save your four app screenshots here, with these exact filenames, so `../index.html`
can display them:

| Filename        | Screenshot to use                                                        |
| --------------- | ------------------------------------------------------------------------ |
| `dashboard.png` | The Dashboard (open flags by severity + active excursions + per-grade)   |
| `trends.png`    | The Trends page with the red "Action-level" tooltip on the Grade A hit   |
| `flags.png`     | The Flags detail with the 7 → … → 60 drift chips + "Generate investigation draft" |
| `data.png`      | The Data page (synthetic dataset + CSV import + results table)           |

PNG or JPG both work (keep the `.png` names, or update the `src` paths in `index.html`).
